[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.media"
[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
numReplicas = 1
